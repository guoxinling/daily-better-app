name: DailyBetter
options:
  minimumXcodeGenVersion: 2.45.3
settings:
  base:
    SWIFT_VERSION: 5.0
    MARKETING_VERSION: 1.1.0
    CURRENT_PROJECT_VERSION: 2
    IPHONEOS_DEPLOYMENT_TARGET: 17.0
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: 6UYTZXY3H9
    TARGETED_DEVICE_FAMILY: "1,2"
targets:
  DailyBetter:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      # Feature source roots include mode-specific check-in composer files.
      - Sources
      - path: Resources/Assets.xcassets
        buildPhase: resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.guoxl.DailyBetter
        PRODUCT_NAME: DailyBetter
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: Daily Better
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.healthcare-fitness
        INFOPLIST_KEY_NSUserNotificationUsageDescription: Daily Better can send one optional daily reminder at the time you choose.
        INFOPLIST_KEY_NSPhotoLibraryUsageDescription: Daily Better lets you choose photos from your library to attach to your private journal entries.
        INFOPLIST_KEY_NSMicrophoneUsageDescription: Daily Better uses the microphone only while you speak to turn your words into editable journal text. Audio is not saved.
        INFOPLIST_KEY_NSSpeechRecognitionUsageDescription: Daily Better uses Apple speech recognition to turn your voice into editable journal text.
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations: UIInterfaceOrientationPortrait
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        DEVELOPMENT_ASSET_PATHS: '"Resources/PreviewContent/Preview Assets.xcassets"'
        SWIFT_EMIT_LOC_STRINGS: YES
  DailyBetterTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - Tests/DailyBetterTests
    dependencies:
      - target: DailyBetter
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
  DailyBetterUITests:
    type: bundle.ui-testing
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - Tests/DailyBetterUITests
    dependencies:
      - target: DailyBetter
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
schemes:
  DailyBetter:
    build:
      targets:
        DailyBetter: all
        DailyBetterTests: [test]
        DailyBetterUITests: [test]
    test:
      gatherCoverageData: true
      targets:
        - DailyBetterTests
        - DailyBetterUITests
